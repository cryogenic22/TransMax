---
document_type: FS
title: Functional Specification - TransMax
release: $release
version: 1.0.0
generated_at: $generated_at
generator: regulatory_pack.generators.pack_builder (TMX-3500)
signed_by: []
traceability_keys:
  - ticket_id
  - ac_id
  - test_function
  - commit_sha
---

# Functional Specification (FS)

**Release:** $release
**Generated:** $generated_at
**Document type:** FS (Functional Specification)
**Standard:** GAMP 5 + EU Annex 11 + 21 CFR Part 11

> **STATUS:** Scaffold v0. Per-URS-Req design rationale is auto-pulled from
> the loop worksheets and the ADR index. Signatures are empty - humans sign
> at release-readiness review.

## 1. Scope

This Functional Specification translates each URS requirement into a
designed implementation. Per GAMP 5, the FS is the source-of-truth for
"what the system does" at the function level. Each FS entry maps backward
to a URS requirement and forward to OQ test cases.

The release this FS describes: **$release**.

## 2. Per-URS-Req functional design

Each shipped ticket is one or more FS-Req entries. The full per-ticket
design is captured in the `.context/loops/TMX-XXXX.md` worksheet's
**Stage 3: Design** section. This document indexes those worksheets and
embeds the bidirectional traceability matrix.

## 3. Architecture decision records (ADR index)

The following ADRs document non-obvious design choices that govern this
release. The full text of each ADR is at `docs/decisions/<filename>`.

$adr_index

## 4. Risk register

A representative subset of the active risk register, drawn from the
2026-05-09 audit and the v3.0 release plan:

- **C-04 / C-05** (Audit ledger v1 weak): chained-hash uses string
  concatenation, no domain separator. Mitigation: TMX-3101 / E2 Audit
  Ledger v2 epic. Until then, the chain is evidence-only, not yet
  defensible. Per addendum A1.
- **C-08** (`QualityGateService` thread-safety): closed by TMX-3053
  (`3ee727e`).
- **C-13** (Auto-promote of high-confidence learning rules): closed by
  TMX-3045 (`7231c7d`).
- **F-H03** (Stored XSS in Tiptap RichTextEditor): closed by TMX-3050
  (`05abca8`).

The complete risk register is maintained in
`research/v3_pilot_ready_release_plan.md` and tracked per-ticket in the
loop worksheets.

## 5. Traceability matrix

Bidirectional mapping: ticket -> AC -> test -> commit. Generated by the
TMX-3500 traceability harness.

Status legend:
- **OK** - test_function and commit_sha both populated
- **NO_TEST** - commit shipped, no test file references the ticket id
- **NO_COMMIT** - test file exists but no commit subject matches the ticket id
- **PLANNING_ONLY** - worksheet declared "Stage 5: N/A" / "no code change"
- **UNVERIFIED** - missing context (planning-only or pre-shipping)

Total rows: **$traceability_row_count**

$traceability_table

## Signatures

- [ ] **Quality Lead** ........................ Date: ............ Signature: ............
- [ ] **Reg Affairs Lead** ..................... Date: ............ Signature: ............

(FS is jointly signed because it bridges the engineering implementation
to the regulatory commitment.)
