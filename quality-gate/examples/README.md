## Quality Gate Example Configs

Copy one of these into your repo root as `.quality-gate.json`, then adjust `paths.exclude` and any rule exceptions for your layout.

Files:
- `basic.json` - core PRS + safe defaults
- `with_evals.json` - adds portable eval runner configuration (requires you to set an executor)
