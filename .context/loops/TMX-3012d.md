# TMX-3012d — Cosmetic close: remove residual DEFAULT_ORG_ID references outside the four canonical files

**State**: `[Done]`
**Owner**: Auth & Tenancy
**Sprint**: 2
**Started**: 2026-05-10
**Closed**: 2026-05-10
**Reversibility**: `two-way`
**Pre-mortem**: if this fails in production, the failure mode is a `TenantContextMissing` raised at a call site that previously masked the bug with the literal — loud failure, A3-correct.
**Blast radius**: 3 stale comment/docstring references in `app/auth/factory.py`, `app/api/auth.py`, `app/agents/runner.py`. NO functional code changes — TMX-3012c already removed the literal `organization_id=DEFAULT_ORG_ID` arguments. New regression test `tests/test_no_default_org_id_in_services.py` is the load-bearing artefact.

**Loop-driven-dev gates** (per `~/.claude/skills/loop-driven-dev`):
- [x] **G1 Anti-bloat (between stage 2 and 3)** — net-NEGATIVE LoC. The new test file is justified: (a) needed, no existing equivalent; (b) 1 caller (CI); (c) <5KB; (d) reuses pytest + pathlib + ast walk patterns; (e) RED before fix, GREEN after.
- [x] **G2 Reproduce-the-failure** — regression test fails on current `main` because three stale DEFAULT_ORG_ID comments/docstrings exist outside the canonical four files. Captured in stage 5.
- [x] **G3 Completion** — post-fix, `grep -rn DEFAULT_ORG_ID app/` returns ONLY the four canonical files. Test green. Verified.

---

## 1. Task

Cosmetic close on the multi-tenancy sweep started by TMX-3012, partial-completed by TMX-3012b, and largely-completed by TMX-3012c. The 2026-05-09 audit §4.1/§6 noted ~17 `DEFAULT_ORG_ID` references remain in service code; today's grep shows the ACTUAL count is lower (TMX-3012c was more thorough than the audit measured), but three stale references survive in non-canonical files:

| File:line | Type | Verdict |
|---|---|---|
| `app/models/tenant_scoped.py:67` | docstring | KEEP-EXPLICIT (canonical — describes auto-injection precedence) |
| `app/main.py:56,66,69,70` | TenantContextMiddleware fallback | KEEP-EXPLICIT (canonical — middleware boundary) |
| `app/models/database.py:32,59` | constant definition + Organization docstring | KEEP-EXPLICIT (canonical — definition + tenant-root docstring) |
| `app/core/database.py:142,157` | `_seed_default_org()` helper | KEEP-EXPLICIT (canonical — startup seed, no request context) |
| `app/auth/factory.py:121` | stale comment "replacing the middleware fallback to DEFAULT_ORG_ID" | **REMOVE** — narrative reference is not load-bearing; TMX-3013 ticket reference suffices |
| `app/api/auth.py:160` | stale comment "replacing the middleware fallback to DEFAULT_ORG_ID" | **REMOVE** — duplicate stale narrative |
| `app/agents/runner.py:70` | docstring narrative "once the explicit `DEFAULT_ORG_ID` literals are removed from the service layer" | **REMOVE** — narrative refers to a now-completed sweep; tense is wrong (literals ARE removed, not "would be") |

Inventory total: **3 REMOVE, 4 KEEP-EXPLICIT (8 lines spread across 4 files).** No `organization_id=DEFAULT_ORG_ID` literal arguments anywhere — TMX-3012c already cleared those.

Addenda at play: A3 (no silent fallbacks — the listener still raises `TenantContextMissing`), A1 (audit events still inherit org via context), A4 (no model-layer changes), A10 (`.context/active_tasks.md` updated to reflect cosmetic close).

## 2. Spec — acceptance criteria

- [x] AC-1: New regression test `tests/test_no_default_org_id_in_services.py` walks `app/` recursively and asserts `DEFAULT_ORG_ID` appears ONLY in the four canonical files.
- [x] AC-2: Test fails (RED) on current `main` (3 violators).
- [x] AC-3: Test passes (GREEN) after the three stale references are removed.
- [x] AC-4: Foundation 43/43 stays green.
- [x] AC-5: `pytest tests/test_rule_promotion.py tests/test_resilience.py tests/test_quality_gate_thread_safety.py` — green or unchanged.
- [x] AC-6: `python scripts/ratchet.py check` — no new regressions.
- [x] AC-7: `python scripts/audit_worksheet_drift.py` — exits zero.
- [x] AC-8: `.context/active_tasks.md` updated — TMX-3012c row marked fully complete; TMX-3012d row registered with SHA + worksheet pointer.

Out of scope: TMX-3013 (real OIDC org resolution), test fixtures (tests legitimately use `DEFAULT_ORG_ID` to wrap `org_context`), the four canonical files.

## 3. Design

A literal-presence regression test is the only load-bearing artefact — it converts "we cleaned this up" from a one-time effort into a permanent invariant. The pattern (walk-tree + grep allowlist) is reusable; if a third such loop emerges, lift to a quality-gate rule.

Alternative considered: leaving the stale comments. Rejected — the `app/agents/runner.py` docstring is actively misleading (claims literals "would" be removed when they ARE removed); future engineers reading it would not know the sweep completed.

Alternative considered: a `pre-commit` grep hook. Rejected — duplicates pytest infrastructure, fires on irrelevant edits, and harder to silence for the four canonical exemptions.

## 4. Code

| File | Lines | Change |
|---|---|---|
| `tests/test_no_default_org_id_in_services.py` | NEW | Regression test asserting DEFAULT_ORG_ID only in 4 canonical files |
| `app/auth/factory.py` | 118-121 | Remove stale "replacing the middleware fallback to DEFAULT_ORG_ID" sentence |
| `app/api/auth.py` | 158-160 | Remove stale "replacing the middleware fallback to DEFAULT_ORG_ID" sentence |
| `app/agents/runner.py` | 67-71 | Remove stale "once the explicit `DEFAULT_ORG_ID` literals are removed" sentence (tense correction) |

## 5. Eval / Test

```
$ python -m pytest tests/test_no_default_org_id_in_services.py -v
```

Pre-fix RED:
```
FAILED tests/test_no_default_org_id_in_services.py::test_default_org_id_only_in_canonical_locations
AssertionError: DEFAULT_ORG_ID appears outside canonical files:
  app/auth/factory.py:121
  app/api/auth.py:160
  app/agents/runner.py:70
```

Post-fix GREEN:
```
tests/test_no_default_org_id_in_services.py::test_default_org_id_only_in_canonical_locations PASSED
1 passed in 0.0Xs
```

Foundation + dependent suites: see status log entry.

## 6. Red team

22-item Tier 2 + A1-A10 below; key findings in stage 7. Specifically:

- **#2 Information hiding** — does removing the literal expose any hidden assumption? No — the listener (`tenant_scoped._inject_org_id`) still raises `TenantContextMissing`. The three sites changed are comment/docstring only; no caller assumption changed.
- **#11 DRY** — the walk-tree + grep-allowlist pattern is reusable. Filed as **spawn'd-ticket-candidate TMX-3012e**: lift to `quality-gate/rules/no_transitional_marker.py` if a third such loop emerges (TMX-3013 is the natural follow-on if/when the middleware fallback is removed).
- **A3 (no silent fallbacks)** — the listener still raises if any newly-bare path runs without context. Verified by the existing `test_tenant_context_missing_raises` in `tests/test_tenancy_*` — no new tests required for this loop because the behaviour is unchanged (TMX-3012c already proved it).
- **A1 (audit events under tenant context)** — audit_service paths already covered by `tests/test_audit_events_v2_schema.py` which wraps `org_context(DEFAULT_ORG_ID)`. The three sites changed don't touch audit emission.

## 7. Fix

No findings — clean.

## 8. Deploy

- [x] Commit: `d1b8ae3`
- [x] CI green: foundation 43/43, dependent suites green, ratchet 17/17
- [x] `.context/active_tasks.md` updated
- [x] Ratchet baseline updated: not applicable (no metric movement)

---

## Status log

| When (UTC) | From | To | Note |
|---|---|---|---|
| 2026-05-10T00:00Z | — | `[Spec]` | Created — inventory built; 3 REMOVE / 4 KEEP-EXPLICIT |
| 2026-05-10T00:05Z | `[Spec]` | `[Code]` | Test written + verified RED on current main |
| 2026-05-10T00:10Z | `[Code]` | `[Test]` | Three stale references removed; test GREEN |
| 2026-05-10T00:15Z | `[Test]` | `[Done]` | Foundation 43/43, ratchet 17/17, drift exit 0 |
