# SEAM-ORCH — autonomous batch execution state (started 2026-07-22)

**Owner**: orchestrator session (Claude). **Read this first when resuming after context loss.**
**Branch**: `feat/vision-delivery-2` — ALL work lands here. Kapil reviews and merges to `main` at the
end; nothing autonomous touches `main`. Board rows therefore stay `WIP (code complete on feat branch)`
until that merge — `DONE` requires the SHA on `origin/main` (push-hygiene rule).

---

## ⚠️ MAIN HISTORY REWRITTEN (2026-08-07) — READ BEFORE ANY MERGE/PUSH-TOWARD-MAIN

Between the last session and 2026-08-07, `origin/main` was **force-updated / rewritten from the root**
(author `stab1`, tip `41fdcc9 chore(security): ignore .env and python caches [STAB-1]`). It is a clean
security purge: `.env` (the leaked OpenAI key, C-01/TMX-3000) is now absent from **every** commit in
main's history, and `.env` is gitignored. New root `a46e8c7` replaces old root `194b84d`; new main is
232 commits. Content delta old-main `0f2bfba` → new main is **only** `.env` deleted + `.gitignore` +2
— all merged work (MQM PR#14, Postgres-CI-green + bcrypt PR#20, auth wall, etc.) is intact; their
messages claim 1375 tests green (unverified here). `transmax.db` is STILL tracked on new main
(TMX-3002 `git rm --cached` half still pending, Kapil-gated).

**Consequence for this branch:** `feat/vision-delivery-2` sits on the OLD, now-orphaned history — it has
**NO common ancestor** with new main, and its ancestry **still contains the leaked `.env`**. It must
**NEVER** be pushed toward / merged into main as-is (that would re-pollute the purged history).

**Reapply path (conflict-free):** the 36 seam commits `0f2bfba..feat/vision-delivery-2` touch neither
`.env` nor `.gitignore`, so they replay onto clean main with zero conflicts. Recommended (non-destructive):
cherry-pick / `git rebase --onto origin/main 0f2bfba feat/vision-delivery-2` onto a **NEW** branch
(e.g. `feat/seam-clean`), verify green (rebuild transmax.db first), and make THAT the review/merge
candidate. Do NOT force-push `feat/vision-delivery-2`; keep it as the safety copy. **Kapil-gated go**
before executing — this is the new step 0, ahead of TM-UNIFY below.

---

## ⏭️ RESUME HERE (updated 2026-07-23, after a context clear)

**HEAD**: `503a68e` on `feat/vision-delivery-2`, **pushed to `origin/feat/vision-delivery-2`** (2026-07-23,
the 8th push attempt succeeded — 34 ahead of `origin/main`). The branch is now backed up and
reviewable on the remote; it is NOT merged to `origin/main`, so per push-hygiene every loop worksheet
correctly stays `[Verify]` and every board row stays `WIP` — `DONE` requires the SHA on `origin/main`,
which is Kapil's merge. The drift auditor's STALE-STATE verdicts are expected and correct here (it
compares against `origin/main`, not the feature branch); do NOT flip them to `[Done]`.
Full suite last green at **1517 passed / 5 skipped / 0 failed**, ratchet **17/17**, frontend
**132/132**. Working tree: only `transmax.db` shows modified (local rebuild artifact — NEVER stage
it, TMX-3002 Kapil-gated). One empty worktree dir remains at `../transmax-wt/` (harmless).

**DONE and integrated on the branch:** Batch A (7 loops) · board-hygiene duplicate-ID sweep +
numeric-SHA parser fix + 13 evidence backfills · Batch B (SEAM-CONTRACT, SEAM-CONFORMANCE,
V1-DURABLE-IR, LANGDETECT-HOLD; C-3 activated) · Batch C/E partial (SEAM-WIRE, LANG-TIERS,
SEAM-MQM-OPTIONAL). **Red-stops cleared: RS-02, RS-06.**

**NEXT ACTIONS, in order:**
1. ~~Retry the push~~ **DONE 2026-07-23** — branch is on `origin/feat/vision-delivery-2` at `503a68e`.
   Keep pushing after each future batch integration. Board rows stay WIP until Kapil merges to main.
2. **Re-run TMX-TM-UNIFY** — died twice (spend limit, then API connection error) with ZERO progress,
   nothing to resume. Fresh worktree at current HEAD. Retire the forked glossary store
   (`glossaries`/`glossary_terms` canonical vs `translation_glossaries` JSON blob); routing/authority
   only, NO destructive migration (A9, Kapil-gated). Brief is in the batch-ce workflow script.
3. **Then TMX-ENF-LEVELS → TMX-PRECEDENCE** (sequential — shared glossary/quality-gate surface;
   parallelising them just manufactures conflicts). Four enforcement levels
   (suggested/preferred/required/locked) not binary `is_strict`; then layered termbase precedence
   (MedDRA/EDQM/QRD/product).
4. **Then TMX-KERNEL-QUALITY last** (deliberately deferred — lowest immediate value, highest conflict
   risk; it is architecture prep for surfaces not yet built).

**Worktree mechanism (Windows):** DO NOT use harness `isolation:'worktree'` — it races on concurrent
mkdir. PRE-CREATE worktrees serially: `git worktree add ../transmax-wt/<slug> -b loop/<slug>` from
current HEAD, pass the absolute path + branch into each agent prompt, agents run WITHOUT isolation.
Loop agents on `model:'sonnet'` (Batch A on Opus cost ~145k tok/loop + hit the spend limit; sonnet
held quality on Batches B and C/E). Orchestrator (this session) stays on Opus for contract/design.

**Permanent orchestrator duties (learned the hard way):**
- Parallel loops CANNOT see each other. Check every new skip-reason / "not built" claim against
  same-batch siblings (C-3 shipped skipped citing LANGDETECT-HOLD, which shipped in the same batch).
- Ratchet regressions come from the loops' OWN new tests (`Any`/`type: ignore`). Fix the test, never
  the baseline. Happened on Batch A (+2 Any) and Batch C/E (+4 type:ignore).
- After merges, rebuild `transmax.db` (`python -c "import os; os.remove('transmax.db') if
  os.path.exists('transmax.db') else None; from app.core.database import init_db; init_db()"`) or
  ~40 stale-schema false-red failures mask the real result.

## reSCApe counterpart status (received 2026-07-23, via Kapil)

reSCApe replied to `Scriptiva_SCA/docs/SEAM-HANDOFF-2026-07-22.md`. Their side:
- **§2 DONE — `534aeed`.** The falsified `transmax_ai` label is fixed on reSCApe's side too. They
  verified the exact chain (`_run_transmax_pipeline`→`translate_text` silent glossary fallback → QC
  gate passing glossary output → hardcoded label at `translate_content:255`), fixed it two ways
  (pipeline calls `_translate_with_llm` directly so fallback→None→honest `glossary_deterministic`;
  `translate_content` surfaces `tm_result.mode`), 6 RED-first tests, satisfies our C-4. **So the
  fail-open label defect is now closed on BOTH sides of the seam** — this is the paired completion of
  the honesty work, independent of our own SDK-FAILCLOSED.
- **§2 docs tracked — `91c9414`** (handoff + RFC-0005 draft, unmodified). §3 freeze honoured (no
  submodule/pin changes).
- **§1 RFC-0005:** committed as draft, awaiting Kapil's signature (unchanged — still the one-way
  blocker). §1 ADR-0009 mirror + backlog rows: deferred to their E8 review-and-merge quiet window
  (correct — `backlog.jsonl` is shared-with-runner, not a drive-by edit).
- **§4 build-next** (webhook receiver, scoped machine creds — their API keys impersonate a full-role
  human `UserAccount`, a real security finding — SEC-AGENT-EGRESS): real platform-team work, gated on
  our contract signal (§5).
- **Their proposal, worth acting on:** they independently flagged the SAME provenance-falsification
  class (their BE-LBL-01 `last_derived_at` advanced without re-derive, BE-LBL-02 coverage over-claims,
  + `transmax_ai`) and propose a **standing guard** — "a provenance value should never be settable
  except on the path that earns it" — plus a C-4-analog conformance test in each repo's OWN CI, not
  only the shared suite. This matches our recurring "unearned claim" theme exactly (5 instances our
  side). **Decision to make at resume:** promote C-4 from the shared seam suite into TransMax's own CI
  gate. Our `tests/seam/test_conformance.py` already encodes C-4; the move is to add it to the CI
  workflow set (eval.yml / a new seam gate) so it runs on every TransMax PR, not just when a live pair
  runs. Small, high-value, aligns with "no vacuous green".

**Contract-signal status (what to tell reSCApe when we signal §5):** `contract/openapi.json` (74
schemas, `x-contract-version: 1.1.0`) IS on `origin/feat/vision-delivery-2` and consumable NOW for
client generation + contract-testing. Caveats to state honestly: (a) on the feature branch, not yet
merged to `main`; (b) live-route wiring is PARTIAL — SEAM-WIRE populated the result block on the live
`GET /result`, but the full `JobResultResponse` shape is additive/not-yet-the-live-surface. So they
can generate a client and contract-test against the artifact now; they should not expect the live
`/result` to return the whole block until merge + the follow-up wiring loop.

## EXCLUDED — needs Kapil / reSCApe, never autonomous:
the push retry is fine to attempt but the
final **merge to `main`** is Kapil's; **RFC-0005** (reSCApe locale, one-way); **TMX-MQM-5b** cutover;
**TMX-3221** identity migration; **TMX-3000** key rotation + **TMX-3002** `git rm --cached`;
`AUTH_MODE` flip; all reSCApe-side edits (handoff doc: `Scriptiva_SCA/docs/SEAM-HANDOFF-2026-07-22.md`).

---

## Phase plan

> **Mechanism note (2026-07-22):** harness worktree isolation raced on Windows (8 concurrent mkdirs
> → EEXIST, run `wf_f89bb0bf-777` died in 2s with 0 agents). Worktrees are now PRE-CREATED manually
> at `../transmax-wt/<slug>` on branches `loop/<slug>` (slugs: posture-fab, scorecard-mock,
> webhook-fire, qrd-fr-heading, tm-fuzzy-honest, sdk-failclosed, langdetect-hold, drift-idlint), all
> based at `3f9f40d`; agents run WITHOUT harness isolation, pinned to their assigned worktree. Live
> run: `wf_44cfbe07-ecf`. At integration: merge `loop/*` branches, then `git worktree remove` each
> and delete `../transmax-wt`.

- **[RUNNING] Batch A** — 8 parallel worktree loops (background workflow `batch-a-truth-loops`):
  TMX-POSTURE-FAB · TMX-SCORECARD-MOCK · TMX-WEBHOOK-FIRE · TMX-QRD-FR-HEADING ·
  TMX-TM-FUZZY-HONEST · TMX-SDK-FAILCLOSED (resumes its stage-2 worksheet) · TMX-LANGDETECT-HOLD ·
  TMX-DRIFT-IDLINT (the scripts-only slice of TMX-BOARD-HYGIENE).
  Agents are FORBIDDEN from editing `.context/active_tasks.md` (shared file — orchestrator owns it)
  and from pushing. Each commits once on its own worktree branch and returns
  `{ticket, status, branch, worktree, sha, files, red/green evidence}`.
- **[NEXT] Integration (orchestrator, serial)** — for each returned branch: `git merge --no-ff`
  (files are disjoint by design; each loop adds its own worksheet + tests), rerun that loop's tests,
  then once all merged: full `python -m pytest tests/ -q`, `cd frontend && npm run typecheck && npx
  vitest run`, `python scripts/ratchet.py check`, `python scripts/audit_worksheet_drift.py`. Fix
  anything red. Update board rows. Remove worktrees.
- **[NEXT] Board hygiene remainder (orchestrator, main tree)** — five-state sweep of
  `active_tasks.md`, stale `pending push` worksheet-header sweep (~52 files), orphan-ID triage table
  (~95 prose-only IDs; default DROPPED unless adopted). The mechanical finder is TMX-DRIFT-IDLINT's
  new `--check-ids` output.
- **[THEN] Batch B (new workflow)** — TMX-SEAM-CONTRACT first (serial: extend `app/schemas/api_v1.py`
  with source-reference block + disposition/MQM/provenance result block; contract version stamp;
  OpenAPI export; TM key `(hash_canonical, target_locale, termbase_version_id)` per ADR-0009 clause
  4) → then TMX-SEAM-CONFORMANCE (invariants C-1..C-9 from CROSS-REPO-PROTOCOL Rule 4; C-1 red test
  first). TMX-V1-DURABLE-IR runs parallel to the contract.
- **[THEN] Batch C** — TMX-KERNEL-QUALITY: extract only the kernel slice the contract exposes
  (`mqm_engine.score()` is already pure — cheapest first).
- **[THEN] Batch E** — TMX-LANG-TIERS · TMX-TM-UNIFY → TMX-PRECEDENCE (sequence these two;
  overlapping glossary surface) · TMX-ENF-LEVELS.
- **[EXCLUDED — needs Kapil or Scriptiva]** — RFC-0005 acceptance (reSCApe locale, one-way);
  TMX-MQM-5b cutover; TMX-3221 execution; TMX-3000/3001/3002; AUTH_MODE flip; all reSCApe-side edits
  (handoff: `Scriptiva_SCA/docs/SEAM-HANDOFF-2026-07-22.md`, sibling checkout); final merge to main.

## Progress log

- **Batch A — DONE, integrated.** 7/8 shipped and merged into `feat/vision-delivery-2` with zero
  conflicts (`f3f9b44`..`1d9b3cd`), integration commit `d7d121f`. Verified: backend **1463 passed /
  0 failed** (the initial 33F/21E were the documented stale-`transmax.db` false-reds — rebuilt per
  the CLAUDE.md procedure), frontend **132/132** + tsc clean, ratchet **17/17** (caught +2 `Any` /
  +1 `type: ignore` from two new test files; fixed by tightening the tests, never the baseline).
  Loop SHAs: POSTURE-FAB `6a8928b` · SCORECARD-MOCK `2294ed4` · WEBHOOK-FIRE `48165cf` ·
  QRD-FR-HEADING `7904396` · TM-FUZZY-HONEST `c186270` · SDK-FAILCLOSED `9c0bf8c` ·
  DRIFT-IDLINT `0c3d1d0`. TMX-LANGDETECT-HOLD died on an account spend limit before starting — no
  work to resume; re-queued into Batch B.
- **Board hygiene — duplicate-ID slice DONE** (`2f05eee`). `--check-ids` found 6 duplicates on its
  first run, three of them introduced by this seam section itself; all 6 cleared, orphan rule
  recorded (97 prose-only ids are DROPPED by default). Remaining: the 52 stale `pending push`
  worksheet headers + 7 stale-state headers.
- **Batch B — RUNNING** (`wf_6e5bd41d-2a7`), worktrees based at `2f05eee`: TMX-SEAM-CONTRACT +
  TMX-V1-DURABLE-IR + TMX-LANGDETECT-HOLD in parallel, then TMX-SEAM-CONFORMANCE serially in the
  contract's worktree (it binds to the contract's actual shipped field names). Loop agents run on a
  cheaper model tier after Batch A cost ~145k tokens/loop and hit the account spend limit.
- **Batch B — DONE, integrated** (`bb6543e`, `c63c541`, `7d4cd39`; C-3 activation `17bee9b`).
  4/4 shipped. TMX-SEAM-CONTRACT `d5ae686` (CONTRACT_VERSION 1.1.0 + result-block models +
  `tm_match_key` + `contract/openapi.json`, 74 schemas, additive/unwired) · TMX-SEAM-CONFORMANCE
  `832e042` (C-1..C-9 registry, meta-test forbids vacuous green) · TMX-V1-DURABLE-IR `367b7b9`
  (IR reconstruction; durable half correctly left to ORCH-CHECKPOINT Loop B) · TMX-LANGDETECT-HOLD
  `b21f2cb` (**RS-06 cleared**). Verified **1499 passed / 5 skipped / 0 failed**, ratchet 17/17
  clean first time.
  *Integration finding:* C-3 shipped SKIPPED citing TMX-LANGDETECT-HOLD as unbuilt — but that
  landed in the SAME batch, and the skipped body asserted against an invented API. Parallel loops
  cannot see each other, so **checking every skip reason against same-batch siblings is a permanent
  orchestrator duty**. Rewritten against the real `DetectionResult.low_confidence` + typed reason;
  suite 12/4 → 14 passed / 3 skipped.
- **Batch C/E — RUNNING** (`wf_aade7cc3-232`), worktrees at `17bee9b`: TMX-SEAM-WIRE (populate the
  result block on the live path from real artefacts only — **`mqm` MUST stay None** until the
  MQM-5b cutover, or it becomes a fifth unearned-claim defect) · TMX-LANG-TIERS (derived
  QUALIFIED/SUPPORTED/AVAILABLE, closes C-9) · TMX-TM-UNIFY (retire the forked glossary store; no
  destructive migration — A9).
  Still queued after: TMX-ENF-LEVELS → TMX-PRECEDENCE (sequence — they share the glossary/quality-gate
  surface with TM-UNIFY), then TMX-KERNEL-QUALITY (architecture prep, deliberately last: it is the
  lowest immediate value and the highest conflict risk).
- **Blocked on network:** `git push origin feat/vision-delivery-2` has failed 6× (HTTP 408 /
  sideband disconnect) despite a 177-object pack with no large blobs. Everything is committed
  locally. Until it lands, the drift auditor correctly reports LOCAL-ONLY and no ticket can be
  `DONE`.

## Merge procedure per agent branch

1. `git merge --no-ff <branch>` from `feat/vision-delivery-2`. Conflicts should not occur (disjoint
   scopes); if one does, resolve preferring both sides, rerun that loop's tests.
2. Verify the loop's red→green evidence against its worksheet before accepting.
3. After all merges: the full-suite + ratchet pass above. A ratchet regression is fixed by tightening
   code, never by loosening the baseline.
4. `git worktree remove` each agent worktree; delete merged branches.
5. Push `feat/vision-delivery-2` to origin so the drift auditor can resolve SHAs.
