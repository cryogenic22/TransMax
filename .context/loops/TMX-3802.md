# TMX-3802 — CJK sentence segmenter (Japanese / Chinese)

**State**: `[Done]` — `942a8cf` on origin/main
**Owner**: Document Pipeline
**Sprint**: 2 (next-10 batch)
**Reversibility**: `two-way` — new segmenter class + registry entries; revert restores the Latin RegexSegmenter fallback.
**Pre-mortem**: mis-splitting CJK could fragment a dosing sentence → tests pin two-sentence splits and decimal-intact single sentences.
**Blast radius**: `app/services/segmenter.py` (new `CJKSegmenter` + ja/zh/zh-cn/zh-tw registry), `tests/test_cjk_segmenter.py`.

**Gates**: G1 ✅ robustness (A3) — the Latin segmenter keys off `[.!?]+\s+`; CJK has no inter-sentence whitespace and ends sentences with 。！？, so a whole CJK paragraph collapsed into one segment and was later LLM-truncated, dropping dosing/safety content. G2 ✅ — the collapse reproduces with the Latin segmenter. G3 ✅ — splits on ideographic terminators, ASCII `.` decimals preserved, Latin path unchanged, KO stays on fallback.

## Spec
- AC-1: ja/zh codes route to `CJKSegmenter`; ko/en stay on `RegexSegmenter`.
- AC-2: `。！？` split sentences with no whitespace required; ASCII decimal (5.5) stays intact.
- AC-3: Latin segmenter behaviour unchanged.

## Test
`tests/test_cjk_segmenter.py` — 6 passed.

## Deploy
- [x] Commit: `942a8cf` (pushed to origin/main)
