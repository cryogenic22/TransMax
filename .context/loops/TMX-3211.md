# TMX-3211 — Replace iteration_count=999 hack with explicit force_finalize flag

**State**: `[Done — pending SHA record]`
**Owner**: Agent & AI
**Sprint**: 2 (10-loop maturity batch)
**Reversibility**: `two-way` — additive state field + one routing branch; replaces a magic sentinel. Revert restores `iteration_count = 999`.
**Pre-mortem**: if this fails, the refinement loop either over-refines (ignores the stop) or finalizes early — both contained to one job; no data/audit impact.
**Blast radius**: `app/agents/graph.py` (TransMaxState +1 field, refine set-site, `decide_next_step` branch), `tests/test_engine_hardening.py`.

**Gates**: G1 ✅ (robustness — removes a magic-number control hack flagged in CLAUDE.md known-issues; clearer + testable). G2 N/A. G3 ✅ — a safety regression sets `force_finalize`; `decide_next_step` routes to `finalize` regardless of iteration count; the `=999` assignment is gone (static guard).

## Spec
- AC-1: `decide_next_step` returns `"finalize"` when `state['force_finalize']` even if status=REVIEW_REQUIRED and iterations<3.
- AC-2: without the flag, REVIEW_REQUIRED + iterations<3 still routes to `refine` (unchanged).
- AC-3: no `iteration_count … = 999` assignment remains in graph.py.

## Test
`tests/test_engine_hardening.py` — force_finalize routing (2) + no-999 static guard. Full suite green.

## Deploy
- [ ] Commit: `<pending>` (bundled with TMX-QG-ESCAPE)
- [ ] Pushed to origin/main

## Status log
| When (UTC) | From | To | Note |
|---|---|---|---|
| 2026-06-11 | — | `[Done — pending SHA record]` | Implemented + tested as part of the 10-loop maturity batch |
